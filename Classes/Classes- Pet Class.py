class Pet:
    def _init_(self, name, animaltype, age):
        self.name = name
        self.animaltype = animaltype
        self.age = age

 '''  def description(self):
        return "{} is {} years old".format(self.name, self.age)

class Doberman(Pet):
    def run(self, speed):
        return "{} runs {}".format(self.name, speed)
        print(Pet)
'''
#dog1 = Pet("Pete")
#dog1.name

    #def setname(self):
     #   return "{} is {} years old".format(self.name, self.age)
        #Ask questions about the get and set command stated in the
        #exercise material. Looks differet from the dcumentation.